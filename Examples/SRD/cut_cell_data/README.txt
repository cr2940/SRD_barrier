To generate your own example, 

	go to set_aux_SRD.f90 (linear) or set_aux_SRD_V.f90 (V)
	change the setting
	run `source geom_calc.sh`

This will produce two files: 

	`small_cells_data.txt`
	`hbox_data.txt`

The pre-existing ones in the folder are for the examples in the previous directory
